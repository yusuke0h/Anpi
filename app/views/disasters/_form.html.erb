<%= simple_form_for @disaster, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name,label:"災害名" %>
  <%= f.input :description, label:"災害情報" %>

  <%= f.button :submit,'新規登録', :class => 'btn-primary' %>
  <%= link_to "キャンセル",
                disasters_path, :class => 'btn btn-default' %>
<% end %>
